(* These tests are type checks, from the point of view of the user
   of the library. This means that this files is supposed to
   be typechecked by being compiled to an .mli file, from the
   point of view of the user of the library, not of the writer of
   the library. *)

let test1 = R.arg R.string